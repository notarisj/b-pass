// ─── Branding config ──────────────────────────────────────────────────────────
// Change these values to rebrand the extension.
// Also update "name" in manifest.json (requires reinstalling the extension).
const APP_BRANDING = {
  name: 'B-Pass1',
  icon: '🔐',
  exportPrefix: 'b-pass1',
};
